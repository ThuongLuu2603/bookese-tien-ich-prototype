// Prototype-only attribution. Production must resolve a server-verified referral.
const partnerQuery=new URLSearchParams(location.search),partnerOfferId=partnerQuery.get('partnerOffer');
function linkedPartnerOffer(){return sourceOffers().items.find(p=>String(p.id)===partnerOfferId&&p.type==='Đại sứ'&&p.ambassadorId===partnerQuery.get('ambassador')&&p.ambassadorCode===partnerQuery.get('ref')&&p.sponsorConsent)}
const beforePartnerQuote=quote;
quote=function(room=customer.room){const offer=linkedPartnerOffer();if(!offer)return beforePartnerQuote(room);const {d,items}=sourceOffers(),m=d.member;return P.quote({items,member:m.tier1&&m.plans[room]?(m.tier2?2:1):0,memberExcluded:m.excluded,maxDiscount:d.maxDiscount},{book:'2026-09-18',stay:customer.stay,nights:nights(),room,ratePlan:'flex',tier:customer.tier,channel:customer.channel||'web',bookingMode:'self',ambassadorId:offer.ambassadorId,ambassadorCode:offer.ambassadorCode,ambassadorVerified:true},Array(nights()).fill(room==='deluxe'?600000:800000))};
const beforePartnerRender=renderCustomer;
renderCustomer=function(){beforePartnerRender();if(!partnerOfferId)return;const p=linkedPartnerOffer(),q=quote(),applied=p&&q.rows?.some(r=>r.best.ps.some(x=>String(x.id)===String(p.id)));const note=document.createElement('div');note.className='offers-bar';note.innerHTML=p?`<b>Được giới thiệu bởi ${esc(p.ambassadorId)} · Mã ${esc(p.ambassadorCode)}</b><p>${applied?'Giá ưu đãi do chỗ nghỉ tài trợ đã được tính vào giá phòng.':'Nguồn Đại sứ đã ghi nhận trong bản thử. Giá bên dưới là phương án tốt nhất đủ điều kiện; mã giới thiệu không đảm bảo mọi phòng/ngày đều được giảm.'}</p><small>Mã Bookese tại thanh toán được xét riêng. Thù lao đối tác không trừ vào quyền lợi khách.</small>`:'Liên kết Đại sứ không có cấu hình phù hợp tại chỗ nghỉ này. Không áp giá Đại sứ.';document.querySelector('#app').prepend(note)};
renderCustomer();
