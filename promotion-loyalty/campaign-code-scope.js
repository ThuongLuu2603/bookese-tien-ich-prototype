const benefitsPromoCheck=P.promo;P.promo=function(total,p){const x=p?.source;if(x?.program){const d=T.read(),ids=d.prices.filter(a=>a.program===x.program&&a.status==='Đang mở').map(a=>a.id),regs=d.registrations.filter(a=>ids.includes(a.price)&&a.status==='Đã tham gia');if(!regs.length||!d.programs.some(a=>a.id===x.program&&a.status==='Đang mở'))return{discount:0,reason:'Chỗ nghỉ chưa được duyệt tham gia chiến dịch đang mở của mã'};}return benefitsPromoCheck(total,p)};
if(targetView==='simulate')updateResults();
