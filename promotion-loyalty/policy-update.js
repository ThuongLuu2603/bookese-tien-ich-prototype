// Approved policy supplement. Demo values are not business defaults.
const P=BookesePolicy;
if(!Number.isFinite(data.maxDiscount))data.maxDiscount=35;
sim.channel='web-mobile';sim.nights=3;sim.nightPrices=[1000000,1200000,900000];
const oldState=localStorage.getItem('bookese-promotions-v3');if(oldState){try{data=JSON.parse(oldState)}catch{}}
data.items.forEach(p=>{if(p.type==='Thứ hạng'&&!p.boostStart){p.status='paused';p.needsSchedule=true}});
let selectedNight=0,selectedPromo='',coinSpend=0;
const coupons=[{code:'BOOKESE100',kind:'amount',value:100000,cap:100000,min:1000000,budget:5000000,quota:50,active:true},{code:'BOOKESE10',kind:'percent',value:10,cap:150000,min:1500000,budget:5000000,quota:50,active:true}];
const offerStorage=()=>{try{return JSON.parse(localStorage.getItem('bookese-prototype-v1'))}catch{return null}};
function syncMember(){let o=offerStorage();if(o){data.member=o.tier1?(o.tier2?2:1):0;if(Number.isFinite(o.maxDiscount))data.maxDiscount=o.maxDiscount;}}
syncMember();
groups.find(g=>g[0]==='market')[1]='Ưu đãi trên di động';groups.find(g=>g[0]==='market')[2]='Web di động và ứng dụng · Quốc gia chưa mở';types.market=['Di động'];
const approvedRules=[['Promo Bookese','Tối đa một mã/đơn; min spend tính trên giá sau ưu đãi, trước Promo/xu; mã % bắt buộc có trần.'],['Bảo vệ giá','Trần tỷ lệ giảm cấp property. Kiểm tra từng phòng/rate plan/đêm, không bù chéo.'],['Lịch áp dụng','Mặc định giảm theo đêm hợp lệ. Yêu cầu cả kỳ lưu trú chỉ khi chương trình cấu hình rõ.'],['Kênh','Web di động và app, chọn riêng hoặc cả hai. Chưa mở giá quốc gia.'],['Ưu đãi Thứ hạng','Giảm sâu độc quyền; nhận đặt tối đa 72 giờ, một đợt mỗi chu kỳ 14 ngày, có hạn lượng phòng; không cam kết top 20.'],['Tham gia chiến dịch','Tự xác nhận khi đạt khung đã duyệt. Ngoại lệ/vị trí giới hạn chuyển duyệt. Tạm ngưng chỉ chặn đơn mới.'],['Hoàn mã','Xử lý theo nguyên nhân; tách quyền dùng của khách và ngân sách hoàn tác.'],['Hoàn xu','Lô còn hạn giữ hạn. Lỗi dịch vụ với lô hết hạn: lô bù 30 ngày. Trả hạn mức chỉ khi cùng tháng sử dụng.'],['Đại lý','30 đơn hoàn tất/quý, bình quân từ 1,2 triệu. Xét và khôi phục quý kế tiếp; không khóa toàn tài khoản.'],['Chi trả','Chờ 30 ngày, tối thiểu 500.000đ, hai kỳ/tháng; thuế chờ Finance xác minh; Giai đoạn 2 mở theo nghiệm thu.']];
const originalRender=render;
render=function(){originalRender();const t=document.querySelector('#main>.tabs');for(const [k,n] of [['returns','Hoàn mã & xu'],['partnerPolicy','Đại lý & chi trả']]){let b=document.createElement('button');b.textContent=n;b.className=view===k?'active':'';b.onclick=()=>openPolicyView(k);t.append(b)}};
function openPolicyView(k){view='rules';render();view=k;document.querySelectorAll('#main>.tabs button').forEach(b=>b.classList.toggle('active',b.textContent===(k==='returns'?'Hoàn mã & xu':'Đại lý & chi trả')));if(k==='returns')renderReturns();else renderPartnerPolicy()}
persist=function(message){data.log.unshift({date:new Date().toLocaleString('vi-VN'),message});try{localStorage.setItem('bookese-promotions-v3',JSON.stringify(data));let o=offerStorage();if(o){o.maxDiscount=data.maxDiscount;localStorage.setItem('bookese-prototype-v1',JSON.stringify(o))}toast('Đã lưu cấu hình mô phỏng')}catch{toast('Không lưu được trên trình duyệt')}if(['returns','partnerPolicy'].includes(view))openPolicyView(view);else render()};
const legacyList=renderList;renderList=function(){legacyList();const cards=$('#content').querySelectorAll('.stat');cards[2].innerHTML=`<span class="small muted">Trần giảm do chỗ nghỉ tài trợ</span><strong>${data.maxDiscount}%</strong><span class="small muted">Cấp property · kiểm tra từng đêm</span><br><button class="quiet" onclick="go('simulate')">Thử giá và điều chỉnh →</button>`;};
const oldEdit=edit;edit=function(id,g='condition',t='Cơ bản'){oldEdit(id,g,t);const p=id?data.items.find(x=>x.id===id):{group:g,type:t};const form=$('#promotionform'),actions=form.querySelector('.actions');let extra=document.createElement('div');extra.innerHTML=`<label class="field">Cách áp lịch lưu trú<select name="wholeStay"><option value="false">Chỉ giảm từng đêm hợp lệ</option><option value="true" ${p.wholeStay?'selected':''}>Toàn bộ kỳ lưu trú phải hợp lệ</option></select></label>${p.type==='Di động'?`<label class="field">Kênh nhận ưu đãi<select name="channel"><option value="both">Web di động và app</option><option value="web-mobile" ${p.channel==='web-mobile'?'selected':''}>Chỉ web di động</option><option value="app" ${p.channel==='app'?'selected':''}>Chỉ app</option></select></label>`:''}${p.type==='Thứ hạng'?`<div class="note">Pilot: tối đa 72 giờ nhận đặt; mỗi đợt cách nhau tối thiểu 14 ngày tính từ lúc bắt đầu. Quyền lợi là nhãn và vị trí ưu đãi được công bố, không cam kết top 20.</div><div class="formgrid"><label class="field">Bắt đầu đợt (giờ Việt Nam)<input name="boostStart" type="datetime-local" value="${p.boostStart||'2026-09-17T00:00'}" required></label><label class="field">Kết thúc đợt<input name="boostEnd" type="datetime-local" value="${p.boostEnd||'2026-09-20T00:00'}" required></label></div><label class="field">Hạn lượng phòng ưu đãi<input name="inventory" type="number" min="1" step="1" value="${p.inventory||10}" required></label>`:''}`;actions.before(extra);const original=form.onsubmit;form.onsubmit=e=>{e.preventDefault();let f=new FormData(form);if(p.type==='Thứ hạng'){let start=new Date(f.get('boostStart')+'+07:00'),end=new Date(f.get('boostEnd')+'+07:00'),h=(end-start)/3600000;let conflict=data.items.some(x=>x.id!==id&&x.type==='Thứ hạng'&&x.boostStart&&Math.abs(start-new Date(x.boostStart+'+07:00'))<14*86400000);if(h<=0||h>72||conflict){$('#formerror').textContent=conflict?'Đợt khác đã nằm trong chu kỳ 14 ngày.':'Cửa sổ nhận đặt phải lớn hơn 0 và tối đa 72 giờ.';return}}original(e);data.items.forEach(x=>{x.wholeStay=x.wholeStay===true||x.wholeStay==='true'});localStorage.setItem('bookese-promotions-v3',JSON.stringify(data));render()};};
function priceInputs(){return sim.nightPrices.map((v,i)=>`<label class="field">${P.date(sim.stay,i)} · Giá gộp (đ)<input aria-label="Giá đêm ${i+1}" type="number" min="1" step="1" value="${v}" oninput="sim.nightPrices[${i}]=Number(this.value);updateResults()"></label>`).join('')}
function changeNights(n){if(!Number.isInteger(n)||n<1||n>31){toast('Bản thử hỗ trợ 1–31 đêm');return}sim.nights=n;sim.nightPrices=Array.from({length:n},(_,i)=>sim.nightPrices[i]||1000000);selectedNight=Math.min(selectedNight,n-1);renderSim()}
renderSim=function(){$('#content').innerHTML=`<div class="workspace" style="grid-template-columns:310px minmax(0,1fr)"><aside class="card"><h2>Tình huống đặt phòng</h2><p class="small muted">Giá minh họa · một phòng, nhiều đêm</p><label class="field">Trần tỷ lệ giảm cấp property (%)<input type="number" min="0" max="99.99" step="0.01" value="${data.maxDiscount}" oninput="data.maxDiscount=this.value===''?NaN:Number(this.value);updateResults()"></label><button class="quiet" onclick="if(Number.isFinite(data.maxDiscount)&&data.maxDiscount>=0&&data.maxDiscount<100)persist('Cập nhật trần tỷ lệ giảm property');else toast('Trần giảm cần từ 0 đến dưới 100%')">Lưu trần giảm</button><div class="note">${Number.isFinite(data.maxDiscount)?data.maxDiscount:0}% là dữ liệu thử, không phải mặc định bắt buộc. Sàn được tính riêng theo giá gốc từng đêm.</div><label class="field">Ngày đặt<input type="date" value="${sim.book}" onchange="sim.book=this.value;updateResults()"></label><label class="field">Nhận phòng<input type="date" value="${sim.stay}" onchange="if(this.value){sim.stay=this.value;renderSim()}"></label><label class="field">Số đêm<input type="number" min="1" max="31" value="${sim.nights}" onchange="changeNights(+this.value)"></label>${priceInputs()}<label class="field">Phòng / loại giá<select onchange="sim.room=this.value;updateResults()"><option value="deluxe" ${sim.room==='deluxe'?'selected':''}>Deluxe · Linh hoạt</option><option value="suite" ${sim.room==='suite'?'selected':''}>Suite · Linh hoạt</option></select></label><label class="field">Hạng khách<select onchange="sim.tier=+this.value;updateResults()">${['Chưa đăng nhập','Thành viên','Thân thiết','Trung thành'].map((x,i)=>`<option value="${i-1}" ${sim.tier===i-1?'selected':''}>${x}</option>`).join('')}</select></label><label class="field">Kênh đặt<select onchange="sim.channel=this.value;updateResults()">${[['web-mobile','Web di động'],['app','Ứng dụng'],['desktop','Máy tính'],['unknown','Không xác định']].map(([k,n])=>`<option value="${k}" ${sim.channel===k?'selected':''}>${n}</option>`).join('')}</select></label><a class="small" href="index.html#offers">Cấu hình thành viên 10%/15% →</a><div class="note">Giá theo quốc gia chưa mở. Backend thật phải xác minh kênh tại checkout.</div></aside><div id="results"></div></div>`;updateResults()};
function configForQuote(){let d={...data};let o=offerStorage();if(o){let plan=o.plans?.[sim.room==='suite'?3:0]||0;d.member=o.tier1&&plan?(o.tier2&&plan===2?2:1):0;}return d}
updateResults=function(){const q=P.quote(configForQuote(),sim,sim.nightPrices);if(q.error){$('#results').innerHTML=`<div class="card error" role="alert">${q.error}</div>`;return}let r=q.rows[Math.min(selectedNight,q.rows.length-1)];$('#results').innerHTML=`<section class="card" style="border-top:3px solid var(--green)"><span class="step">GIÁ BÁN ĐƯỢC CHỌN · ${sim.nights} ĐÊM</span><h2 style="font-size:34px;color:var(--blue)">${money(q.total)}</h2><p class="muted small">Giá gốc ${money(q.base)} · trước mã Bookese và xu. Mỗi đêm chọn tổ hợp hợp lệ riêng.</p><div class="tablewrap"><table><thead><tr><th>Đêm</th><th>Giá gốc</th><th>Sàn suy ra</th><th>Nhánh</th><th>Giá chọn</th></tr></thead><tbody>${q.rows.map((x,i)=>`<tr><td><button class="quiet" onclick="selectedNight=${i};updateResults()">${x.date}</button></td><td>${money(x.base)}</td><td>${money(x.floor)}</td><td>${x.best.branch}</td><td><b>${money(x.best.price)}</b></td></tr>`).join('')}</tbody></table></div></section><section class="card"><h2>Chi tiết đêm ${r.date}</h2><p class="small muted">Bấm ngày trong bảng để xem các tổ hợp của đêm đó.</p><table><thead><tr><th>Tổ hợp</th><th>Giá</th><th>Kết quả</th></tr></thead><tbody>${r.combos.map(c=>`<tr ${c===r.best?'style="background:#edf8f2"':''}><td>${c.branch} · ${c.ps.map(p=>esc(p.name)+' '+p.rate+'%').join(' + ')||'Giá gốc'}</td><td>${money(c.price)}</td><td>${!c.ok?'Dưới sàn':c===r.best?'✓ Được chọn':'Giá cao hơn/bằng'}</td></tr>`).join('')}</tbody></table><details><summary style="margin-top:15px">Ưu đãi không đủ điều kiện (${r.excluded.length})</summary>${r.excluded.map(x=>`<p class="small">${esc(x.name)}: ${x.reason}</p>`).join('')}</details></section><section class="card"><h2>Mã giảm giá Bookese</h2><p class="small muted">Một mã/đơn. Cơ sở min spend là ${money(q.total)}, sau ưu đãi chỗ nghỉ, trước Promo và xu. Mã và ngân sách bên dưới là ví dụ.</p><label class="field">Chọn mã<select onchange="selectedPromo=this.value;updateResults()"><option value="">Không sử dụng mã</option>${coupons.map(p=>{let a=P.promo(q.total,p);return `<option value="${p.code}" ${selectedPromo===p.code?'selected':''} ${a.reason?'disabled':''}>${p.code} · ${a.reason||'Giảm '+money(a.discount)}</option>`}).join('')}</select></label><button class="quiet" onclick="chooseBestPromo()">Chọn mã giảm nhiều nhất</button>${checkoutV3(q.total)}</section>`};
function chooseBestPromo(){const q=P.quote(configForQuote(),sim,sim.nightPrices);if(q.error)return;const best=coupons.filter(p=>!P.promo(q.total,p).reason).sort((a,b)=>P.promo(q.total,b).discount-P.promo(q.total,a).discount)[0];selectedPromo=best?.code||'';updateResults()}
function checkoutV3(total){let p=coupons.find(p=>p.code===selectedPromo),discount=P.promo(total,p),cap=Math.min(Math.floor(total*.3),2000000,5000000),valid=Number.isSafeInteger(coinSpend)&&coinSpend>=0&&coinSpend<=cap&&coinSpend+discount.discount<=total&&(sim.tier>=0||coinSpend===0),pay=total-discount.discount-coinSpend;return `<div class="note">${p?`${p.code}: ${p.kind==='percent'?p.value+'%, trần '+money(p.cap):money(p.value)} · Đơn từ ${money(p.min)}`:'Chưa chọn mã'}</div>${discount.reason?`<div class="error">${discount.reason}; không áp dụng khoản giảm này.</div>`:''}<label class="field">Xu muốn cấn trừ<input type="number" min="0" step="1" value="${coinSpend}" onchange="coinSpend=Number(this.value);updateResults()"></label><p class="small muted">Trần đơn ${money(cap)}; ví dụ giả định đủ số dư và còn hạn mức tháng.</p>${valid?`<div class="price-line"><span>Bookese tài trợ</span><b>−${money(discount.discount)}</b></div><div class="price-line total"><span>Khách trả</span><b>${money(pay)}</b></div><div class="reward">Xu dự kiến: ${Math.floor(pay*(sim.tier<0?0:sim.tier+1)/100).toLocaleString('vi-VN')}</div>`:'<div class="error">Xu không hợp lệ, vượt trần/tổng cộng hoặc khách chưa đăng nhập.</div>'}`}
const legacyRules=renderRules;renderRules=function(){legacyRules();$('#content').insertAdjacentHTML('beforeend',`<section class="card"><h2>10 quyết định đã duyệt</h2>${approvedRules.map(([n,d])=>`<h3 style="margin-top:18px">${n}</h3><p class="small muted">${d}</p>`).join('')}</section>`)};
function renderReturns(){$('#content').innerHTML=`<section class="card"><h2>Mô phỏng hoàn mã và xu</h2><p class="muted">Chọn nguyên nhân để xem quyền dùng mã, xu trả lại và hạn mức tháng.</p><div class="formgrid"><label class="field">Nguyên nhân<select id="returnCause" onchange="resultReturn()"><option value="free">Khách hủy miễn phí, hoàn toàn bộ</option><option value="service">Lỗi Bookese / nhà cung cấp</option><option value="fee">Hủy có phí / no-show</option><option value="partial">Hoàn một phần</option><option value="amend">Sửa đơn</option><option value="failed">Thất bại trước xác nhận</option></select></label><label class="field">Mã còn hạn?<select id="codeValid" onchange="resultReturn()"><option value="yes">Còn hạn</option><option value="no">Đã hết hạn</option></select></label><label class="field">Xu đã cấn trên phần đơn gốc<input id="spent" type="number" min="0" value="100000" oninput="resultReturn()"></label><label class="field">Xu đã cấp do đơn sinh ra<input id="earned" type="number" min="0" value="15000" oninput="resultReturn()"></label><label class="field">Tỷ lệ phần giá trị được hoàn (%)<input id="ratio" type="number" min="0" max="100" value="100" oninput="resultReturn()"></label><label class="field">Lô xu gốc<select id="expired" onchange="resultReturn()"><option value="no">Còn hạn</option><option value="yes">Đã hết hạn</option></select></label><label class="field">Thời điểm hoàn<select id="sameMonth" onchange="resultReturn()"><option value="yes">Cùng tháng sử dụng xu</option><option value="no">Sang tháng sau</option></select></label></div><div id="returnResult"></div></section>`;resultReturn()}
function resultReturn(){const cause=$('#returnCause').value,v={spent:+$('#spent').value,earned:+$('#earned').value,ratio:+$('#ratio').value,expired:$('#expired').value==='yes',cause:cause==='service'?'service':'customer',sameMonth:$('#sameMonth').value==='yes'};if(!Number.isSafeInteger(v.spent)||!Number.isSafeInteger(v.earned)||v.spent<0||v.earned<0||v.ratio<0||v.ratio>100){$('#returnResult').innerHTML='<p class="error">Kiểm tra số xu và tỷ lệ 0–100%.</p>';return}const r=P.returnCoins(v),pr=P.promoReturn(cause,$('#codeValid').value==='yes');$('#returnResult').innerHTML=`<div class="note">Mã: ${pr[0]}. Ngân sách: ${pr[1]}.</div>${cause==='failed'?'<p class="small">Giao dịch chưa xác nhận: giải phóng phần giữ, không ghi dùng mã hoặc cấp xu.</p>':`<table><tbody><tr><th>Xu trả lại theo tỷ lệ</th><td>${r.back.toLocaleString('vi-VN')}</td></tr><tr><th>Xu khả dụng sau trả</th><td>${r.available.toLocaleString('vi-VN')}</td></tr><tr><th>Phần hết hạn không gia hạn</th><td>${r.expired.toLocaleString('vi-VN')}</td></tr><tr><th>Lô bù do lỗi dịch vụ</th><td>${r.compensation.toLocaleString('vi-VN')} · ${r.expiry}</td></tr><tr><th>Xu đã cấp cần thu hồi</th><td>${r.revoke.toLocaleString('vi-VN')}</td></tr><tr><th>Hạn mức tháng được trả</th><td>${r.monthQuota.toLocaleString('vi-VN')}</td></tr></tbody></table><div class="note">Tỷ lệ hoàn ở đây phải lấy từ kết quả xử lý đơn, không mặc định cứ hủy là hoàn 100%. Nhiều lần hoàn tính lũy kế và trừ phần đã xử lý. Sửa đơn cần kiểm lại mức hoàn và giá mới.</div>`}`}
function renderPartnerPolicy(){$('#content').innerHTML=`<section class="card"><h2>Đại lý và chi trả thù lao</h2><div class="rules"><div class="rule">Duy trì<b>30 đơn/quý · Bình quân ≥ 1,2 triệu</b></div><div class="rule">Bậc thù lao<b>5% → 6% (60 đơn) → 7% (120 đơn)</b></div><div class="rule">Khôi phục<b>Áp từ quý kế tiếp</b></div><div class="rule">Rút thù lao<b>Chờ 30 ngày · Tối thiểu 500.000đ</b></div></div><p style="margin-top:18px">Không đạt duy trì không khóa toàn tài khoản; giữ quyền đặt/xem đơn và thù lao đã phát sinh. Mốc 20 đơn chỉ dùng nhắc tiến độ.</p><div class="note">Hai kỳ chi mỗi tháng. Thuế phải được Finance xác minh trước chi trả; không dùng mặc định mức thuế trong hồ sơ cũ.</div></section><section class="card"><h2>Điều kiện mở Giai đoạn 2</h2>${['Ghi nhận đúng kênh và tách đúng nguồn số dư','Nghiệm thu hoàn, hủy, thu hồi','Duyệt hồ sơ partner, điều khoản và thuế','Nghiệm thu lập, duyệt chi và đối soát','Pilot hoàn thành theo tiêu chí được duyệt'].map(x=>`<p>○ ${x}</p>`).join('')}<div class="note">Chưa ấn định ngày mở. Có thể mở ghi nhận trước, rút sau chu kỳ đối soát pilot; phải công bố điều kiện trước khi tham gia.</div></section>`}
render();
